SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO


-- Inserts a new record into the 'USERSPROFILES' table.
CREATE PROCEDURE [dbo].[_USERSPROFILES_Insert]
	@USERID int,
	@SUBSCRIPTIONID int,
	@ACTIVE bit,
	@DATESTART date,
	@DATEND date,
	@FILTER varchar(max),
	@INVISIBLE bit
AS
	INSERT INTO [dbo].[USERSPROFILES]
	(
		[USERID],
		[SUBSCRIPTIONID],
		[ACTIVE],
		[DATESTART],
		[DATEND],
		[FILTER],
		[INVISIBLE]
	)
	VALUES
	(
		@USERID,
		@SUBSCRIPTIONID,
		@ACTIVE,
		@DATESTART,
		@DATEND,
		@FILTER,
		@INVISIBLE
	)
	SELECT @@IDENTITY
GO
